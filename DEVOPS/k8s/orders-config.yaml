apiVersion: v1
kind: ConfigMap
metadata:
  name: orders-config
data:
  DB_HOST: "postgres-service"
  DB_NAME: "loja_db"
---
apiVersion: v1
kind: Secret
metadata:
  name: orders-secrets
type: Opaque
data:
  # echo -n 'admin' | base64
  DB_USER: YWRtaW4=
  # echo -n 'secret' | base64
  DB_PASS: c2VjcmV0
